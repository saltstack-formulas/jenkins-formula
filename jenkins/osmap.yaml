Debian:
    netcat_pkg: netcat-openbsd
    netcat_flag: -z

RedHat:
    netcat_pkg: nmap-ncat
    netcat_flag: --recv-only

FreeBSD:
    shell: /bin/sh
    # FreeBSD comes with nc on board
    netcat_pkg: False
    netcat_flag: -z
